# =============================================================================
# EXAMPLE ONLY — DO NOT APPLY DIRECTLY
# =============================================================================
# hermes-backup-secret key sozlesmesi. Gercek degerler Git'te TUTULMAZ.
# Apply etmeden once docs/security/runtime-secret-contract.md okuyun.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: hermes-backup-secret
  namespace: hermes-dev
type: Opaque
stringData:
  AZURE_CLIENT_ID: REQUIRED_FROM_OPERATOR
  AZURE_CLIENT_SECRET: REQUIRED_FROM_OPERATOR
  AZURE_TENANT_ID: REQUIRED_FROM_OPERATOR
  ONEDRIVE_USER: REQUIRED_FROM_OPERATOR
  DB_PASSWORD: REQUIRED_FROM_OPERATOR
